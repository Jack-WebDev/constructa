{
  "name": "constructa",
  "description": "A composable platform for building reusable data generators.",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "check-types": "turbo run check-types",
    "dev:web": "turbo run dev -F web",
    "check": "biome check . && pnpm run check:architecture && pnpm run check:jsr",
    "check:architecture": "node scripts/check-architecture.mjs",
    "check:jsr": "node scripts/sync-jsr-versions.mjs --check",
    "ci": "pnpm run check && pnpm run check-types && pnpm run test && pnpm run test:release && pnpm run test:release:jsr",
    "check:fix": "biome check --write .",
    "lint": "biome check --write .",
    "test": "pnpm --filter constructa-sdk build && vitest run",
    "test:release": "pnpm --filter './packages/*' build && node scripts/check-release-artifacts.mjs",
    "test:release:jsr": "node scripts/sync-jsr-versions.mjs --check && node scripts/publish-jsr.mjs --dry-run --allow-dirty",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "benchmark:core": "node scripts/core-performance-baseline.mjs",
    "changeset": "changeset",
    "changeset:status": "changeset status",
    "version-packages": "changeset version && node scripts/sync-jsr-versions.mjs",
    "release:version": "pnpm run version-packages",
    "release:publish": "pnpm run build && changeset publish",
    "release:npm": "pnpm run build && changeset publish",
    "release:jsr": "node scripts/sync-jsr-versions.mjs && node scripts/publish-jsr.mjs",
    "release:jsr:dry-run": "node scripts/sync-jsr-versions.mjs && node scripts/publish-jsr.mjs --dry-run",
    "prepare": "husky"
  },
  "dependencies": {
    "@constructa/env": "workspace:*",
    "dotenv": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.11",
    "@changesets/cli": "^3.0.1",
    "@constructa/config": "workspace:*",
    "@types/node": "catalog:",
    "@vitest/coverage-v8": "catalog:",
    "husky": "^9.1.7",
    "jsdom": "catalog:",
    "jsr": "^0.14.3",
    "lint-staged": "^17.4.1",
    "publint": "^0.3.24",
    "tsdown": "catalog:",
    "turbo": "^2.10.12",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "lint-staged": {
    "*.{js,ts,cjs,mjs,d.cts,d.mts,jsx,tsx,json,jsonc}": [
      "biome check --write --no-errors-on-unmatched"
    ]
  },
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=10.0.0 <11.0.0"
  },
  "packageManager": "pnpm@10.32.1"
}
