---
"constructa-sdk": minor
"constructa-generators": minor
"constructa-schema": minor
---

Add SDK `generate()` and serialization utilities, plus portable semantic metadata for the built-in generator catalog.
