---
"constructa-schema": major
---

Replace the `{ type, configuration }` envelope with versioned generator documents containing flat generator definitions. Use `parseDocument` and the new document validation APIs; the retired envelope APIs are no longer exported.
