# `@constructa/exporters`

Output serialization and export formats for generated data.

Planned formats include JSON, JSON Lines, CSV, and SQL. Exporters consume generated values and formatting options; they do not execute generators or contain UI and download behavior.
