# Examples

Runnable examples demonstrating Constructa's public APIs will live here.

Examples should use published package entry points rather than internal source paths. Initial candidates are primitive generation, nested object generation, deterministic fixtures, bulk output, and CLI definition files.
